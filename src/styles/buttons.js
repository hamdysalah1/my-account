import { css } from 'styled-components';
import { PRIMARY_COLOR, BORDER_RADIUS } from '../constants'
export const Button = css`
  background-color: ${PRIMARY_COLOR};
  border-radius: ${BORDER_RADIUS};
`;
